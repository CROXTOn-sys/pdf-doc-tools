# PDF -> DOCX conversion is handled by LibreOffice in headless mode, invoked as
# a system binary (soffice) from converter.py. There are no Python package
# dependencies for the converter itself.
#
# System requirements (install on the server, NOT via pip):
#   - LibreOffice (provides the `soffice` binary)
#   - Indic/Unicode fonts so all scripts render correctly (e.g. Noto or Lohit)
#
# See backend/README.md for the exact install commands.
