# Primary PDF -> DOCX engine (best layout/table/image reconstruction).
# Installed from the pinned GitHub source that was verified to work.
pdf2docx-plus @ git+https://github.com/mithunvoe/pdf2docx-plus.git

# PyMuPDF (fitz) is used to detect Indic scripts for engine selection. It is
# normally installed as a dependency of pdf2docx-plus; listed here to be safe.
PyMuPDF

# NOTE: LibreOffice is ALSO required as a system package (not pip). It is used
# as the fallback engine for Indic-language PDFs. See backend/README.md.
